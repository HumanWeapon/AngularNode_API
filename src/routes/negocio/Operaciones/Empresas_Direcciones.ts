import { Router } from 'express';
import {validarToken} from '../../validarToken';


const empresasDirecciones = Router();

//empresasDirecciones.get('/consultarContactosNoRegistradosPorId/:id', validarToken, consultarContactosNoRegistradosPorId);
//empresasDirecciones.get('/consultarContactosActivosporId/:id', validarToken, consultarContactosActivosporId);
//empresasDirecciones.post('/agregarOperacionEmpresaContacto', validarToken, agregarOperacionEmpresaContacto);
//empresasDirecciones.delete('/eliminarOperacionEmpresaContacto/:id', validarToken, eliminarOperacionEmpresaContacto);

export default empresasDirecciones;
